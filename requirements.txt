# Held to one major, because the server API the code targets is that major's.
# This file is the Render deploy manifest, so the upper bound is what stops the
# next major from reaching the live server before anyone has ported to it.
mcp>=2.0.0,<3
pymupdf>=1.24.0
starlette>=0.37.0
uvicorn>=0.30.0
