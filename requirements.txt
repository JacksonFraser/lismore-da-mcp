# Capped below 2.0: it renames Tool.inputSchema to input_schema, which every
# tool registration here goes through. This file is the Render deploy manifest,
# so an uncapped range meant the next deploy of the live server would install a
# major version it has not been ported to. Migrating is separate work.
mcp>=1.0.0,<2
pymupdf>=1.24.0
starlette>=0.37.0
uvicorn>=0.30.0
